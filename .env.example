# Candy Couture — environment variables
# Copy this file to .env.local for local development, and set the same
# values in Vercel → Project → Settings → Environment Variables.

# WhatsApp number that receives orders (international format, digits only,
# no "+" or spaces). Example for Bahrain: 973XXXXXXXX
NEXT_PUBLIC_WHATSAPP_NUMBER=XXXXXXXXXXX
