# Normalize line endings to LF in the repo (Linux/Vercel build consistency)
* text=auto eol=lf

# Binary assets — never normalize
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.avif binary
*.ico binary
